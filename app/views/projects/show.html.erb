<p>
  <b>Title:</b>
  <%= @project.title %>
</p>

<%= render :partial => 'roles', :collection => @project.roles %>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
